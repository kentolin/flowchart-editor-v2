### **ShapeLoader** - Simplify:

---

- Load config.json via fetch
- Import RectShape.js via dynamic import
- Create ShapeDefinition(config, RectShape)
- Register in ShapeRegistry

**config.json** - Keep ports, handles, constraints - NodeView uses these
